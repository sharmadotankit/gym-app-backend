<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Email OTP</title>
    <style>
        /* Reset some default styles to ensure consistent rendering */
        body, p {
            margin: 0;
            padding: 0;
        }

        /* Set a background color and font for the email body */
        body {
            background-color: #f2f2f2;
            font-family: Arial, sans-serif;
        }

        /* Container for the OTP content */
        .container {
            width: 600px;
            margin: 0 auto;
            padding: 20px;
            background-color: #fbffc7;
            box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.575);
            text-align: center;
        }

        /* Header styling */
        .header {
            text-align: center;
            margin-bottom: 20px;
        }

        .header h1 {
            color: #333;
        }

        /* OTP styling */
        .otp {
            font-size: 24px;
            font-weight: bold;
            color: #007bff;
        }



        /* Add some padding and margin to paragraphs */
        p {
            margin-bottom: 10px;
            padding: 0 10px;
        }

    </style>
</head>
<body>
    <div class="container" >
        <div class="header">
            <h1>Reset Password Otp</h1>
        </div>
        <p>Hello <%= data.userName %>,</p>
        <p>Please enter otp below to reset you password:</p>
        <p class="otp"><%= data.otp %></p>
        <p>Please use this OTP to change password for you account.</p>
        <p>Thank you,</p>
        <p>Gym Applicaton Support Team</p>
    </div>
</body>
</html>
